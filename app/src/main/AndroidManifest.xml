<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sensei374121.amey.assignment2demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
       <!-- android:theme="@style/AppTheme"> -->
        <activity android:name=".MainActivity" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".linear_activity" android:theme="@style/CustomTheme1" android:label="Linear Activity"></activity>
        <activity android:name=".relative_activity" android:theme="@style/CustomTheme1" android:label="Relative Activity"></activity>
        <activity android:name=".grid_activity" android:theme="@style/CustomTheme2" android:label="Calculator"></activity>
        <activity android:name=".control_and_event_activity" android:theme="@style/CustomTheme2" android:label="Control and Events"></activity>
    </application>

</manifest>
